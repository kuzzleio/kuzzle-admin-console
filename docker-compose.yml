kuzzle:
  image: kuzzleio/kuzzle:beta
  ports:
    - "7512:7512"
    - "7511:7511"
  links:
    - elasticsearch
    - redis
  environment:
    - LIKE_A_VIRGIN
    - FIXTURES
    - DEFAULT_MAPPING
    - FEATURE_COVERAGE

kuzzleBo:
  image: kuzzleio/bo:beta
  ports:
    - "3000:3000"
  links:
    - kuzzle

redis:
  image: redis:3.0

elasticsearch:
  image: elasticsearch:1.7
