export enum KKuzzleGettersTypes {
  WRAPPER = 'wrapper',
  $KUZZLE = '$kuzzle',
  CURRENT_ENVIRONMENT = 'currentEnvironment',
  IS_CURRENT_ENVIRONMENT_VALID = 'isCurrentEnvironmentValid',
  HAS_ENVIRONMENT = 'hasEnvironment',
  ENVIRONMENTS = 'environments',
}
