version: '3'

services:
  kuzzle:
    image: kuzzleio/kuzzle:2.12.1
    ports:
      - "${KUZZLE_PORT:-7512}:7512"
    cap_add:
      - SYS_PTRACE
    depends_on:
      - redis
      - elasticsearch
    environment:
      - kuzzle_services__storageEngine__client__node=http://elasticsearch:9200
      - kuzzle_services__storageEngine__commonMapping__dynamic=true
      - kuzzle_services__storageEngine__internalIndex__collections__users__dynamic=true
      - kuzzle_services__internalCache__node__host=redis
      - kuzzle_services__memoryStorage__node__host=redis
      - NODE_ENV=development

  redis:
    image: redis:5

  elasticsearch:
    image: kuzzleio/elasticsearch:7.4.0
    ulimits:
      nofile: 65536
    environment:
      - "ES_JAVA_OPTS=${ES_JAVA_OPTS:--Xms1024m -Xmx1024m}"
